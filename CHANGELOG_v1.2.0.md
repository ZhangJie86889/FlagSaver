# FlagSaver v1.2.0 更新说明 —— 月历视图

本版新增一个**月历视图**：可以在日历上点某一天，看这一天的任务，并直接为这一天添加任务。
原有「截止日倒计时」的语义、卡片样式、快捷键、设置面板全部保持不变。

改动只落在 `flag.html`（+ `README.md` / `build_zip.py` / 示例数据）；
`saver_window.py` **只改了版本号**，追加了一段说明注释，逻辑一行未动 —— 原因见文末「为什么没改后端」。

---

## 一、数据模型

Flag 对象新增一个字段：

| 字段 | 类型 | 含义 |
|---|---|---|
| `id` | string | 主键 |
| `name` | string | 名称 |
| `date` | `"YYYY-MM-DD"` | **截止日**（原有字段，语义不变） |
| **`plan_date`** | `"YYYY-MM-DD"` | **新增：计划在哪一天执行**（月历按它归组） |
| `completed` | bool | 是否完成 |
| `subtasks` | `Array<{id,text,done}>` | 子任务 |

`date` 与 `plan_date` 是**两个独立语义**，不混用、不互相覆盖：

- `date` = 这件事什么时候到期（卡片上的「🎯 截止 …」与剩余天数）
- `plan_date` = 打算哪一天动手（月历上的位置）

两者可以不同。例如「计划 12-20 复盘错题，截止 12-30」，它会出现在月历的 12-20 那天，
卡片上同时显示 `📅 计划 2026-12-20` 和 `🎯 截止 2026-12-30`。

### 向后兼容

- 读取时 `plan_date` 缺失（老数据）→ **回退到原有 `date`**。
  所以升级后旧 Flag 会照常出现在它原来那个日期的格子上，不报错、不消失、不需要迁移。
- `normalize()` 是字段白名单重建，本版把 `plan_date` 加进了白名单；
  老数据的 `plan_date` 会被补成空串 `""`（**不会**被回填成 `date`，回退只发生在读取时），
  原有字段一律原样保留。
- 建新 Flag 时如果不指定 `plan_date`（比如继续用底部输入框添加），卡片行为与以前完全一致。

---

## 二、月历视图

### 位置与布局

放在 Flag 卡片网格**下方**、底部添加区之上，随内容区一起受 `--content-max: 1400px` 限宽居中。
刻意放在卡片下方，是为了不打扰屏保模式下「卡片是主角」的既有视觉。

### 月历

- 周一起首（与中文习惯一致），月初/月末用留白占位，保证列永远不错位。
- 今天：日期数字用暖色 `--color-warn` + 底部一条横条 + `aria-current="date"`，**不依赖单一颜色**。
- 上方有「今天」按钮和 ‹ › 上下月切换；标题右侧显示本月任务条数。
- 有任务的日期：**一个圆点 + 任务数量**（双通道：颜色之外还有数字）；
  当天任务全部完成时圆点从强调色变成成功色 `--color-success`。
- 日期格全部是 `<button>`，键盘可达（见下）。

### 当天面板

点日期后在月历下方展开一个内联面板（非模态，`role="dialog" aria-modal="false"`）：

- **任务列表**：标题、剩余天数/逾期天数、子任务勾选、完成态样式。
  完成态完全复用卡片那套「**颜色 + 对勾图标 + 「已完成」文字**」三重标识（`.done-badge`）。
- **＋ 添加当天任务**：展开内联表单 —— 名称（必填）、截止日（可选，留空＝当天）、子任务（可选，一行一条）。
- 保存后任务**立即**出现在当天列表里，月历上的圆点/数量也同步刷新，面板保持打开。

校验规则与底部表单**完全一致**（同一套函数与样式）：

| 情况 | 表现 |
|---|---|
| 名称为空 | 输入框下方内联报错「请填写任务名称」+ 红框 + 抖动 |
| 截止日格式错 | 内联报错（复用 `dateFieldError()`） |
| 截止日早于今天 | 内联报错「截止日期不能早于今天（今天是 …）」 |
| 计划日已过去 | 面板顶部警示行 + 提交时内联报错并阻止写入 |

错误节点用的是和现有表单同一个 `.field-error`（`role="alert"`），不是另起一套。

---

## 三、键盘与可访问性

### 键盘

| 按键 | 行为 |
|---|---|
| `Tab` | 进入/离开月历（roving tabindex：整个网格只有一个 Tab 停靠点，不会陷入 30 次 Tab） |
| `←` `→` | 前一天 / 后一天（跨月会自动切换月份） |
| `↑` `↓` | 前一周 / 后一周 |
| `Home` / `End` | 本周一 / 本周日 |
| `PageUp` / `PageDown` | 上一个月 / 下一个月（月末自动收敛，1/31 → 2/28） |
| `Enter` / `Space` | 打开当天面板（交给 `<button>` 原生行为，不重复拦截） |
| `Esc` | 关闭当天面板；**再按一次**才隐藏屏保 |

### Esc 冲突（本版最容易踩的坑）

本项目里 `Esc` 的默认语义是「隐藏整个屏保」，而当天面板打开时 `Esc` 只能关面板。
实现方式：

1. 面板的 `Esc` 监听器注册在 **capture 阶段**，命中后 `stopImmediatePropagation()`，
   掐断后续所有监听器（包括 `patchSaverExit()` 里那个异步注册的 capture 监听器，
   以及 `document` 上冒泡阶段的「退出屏保」）。
2. 同时在 `patchSaverExit()` 的 handler 里加了一道守卫：面板打开时直接放行、不调 `quit_saver()`。
   双保险，防止将来监听器注册顺序变化时把屏保一起关掉。
3. 面板一关闭，两个判断都立刻失效，**原有 Esc 行为原样恢复**。

设置面板（⚙）的 `Esc` 优先级高于当天面板，各管各的。

### 焦点管理

- 打开面板 → 焦点进入面板（`tabindex="-1"` + `.focus()`），并把面板滚进可视区。
- 关闭面板 → 焦点**还原到触发的那个日期格子**（网格是整块重绘的，按 `data-date` 重新找回）。
  为此还专门保证 `cal.focusDate` 始终落在当前显示的月份里，否则所有格子都会是 `tabindex="-1"`，
  `Tab` 就再也进不来月历了。

### 其它

- **状态播报**：`role="status" aria-live="polite"` 的 `sr-only` 区域会播报
  切换月份、打开/关闭面板、新增/删除/完成任务的摘要。
- **日期格的无障碍名称**：`aria-label` 里一次说清「年月日 + 周几 + 今天 + 任务数 + 未完成数」，
  读屏用户不必点进去才知道那天有没有任务。
- **`prefers-reduced-motion`**：面板/任务项的滚入动画统一走
  `matchMedia('(prefers-reduced-motion: reduce)')` 判断，开启后改为瞬时定位
  （CSS 侧的全局 reduce 规则照旧生效）。

---

## 四、设计 Token

新代码**没有写死任何 `#hex` 或零散 px**，全部引用 v1.1.1 建立的 `:root` Token，
另外补了 5 个新 Token：

| 新 Token | 值 | 用途 |
|---|---|---|
| `--color-accent-a10 / a18 / a30 / a45` | accent 的透明度阶梯 | 月历悬停/选中/按钮底色，替代各处手写 `rgba(124,140,255,…)` |
| `--glass-bg-hover` | `rgba(255,255,255,.16)` | 月历导航按钮、面板关闭按钮的悬停底色 |
| `--color-border-region` | `rgba(124,140,255,.62)` | **内联面板的边界**。面板底色与页面底只差 1.08:1，边界全靠这条描边识别，因此不能按「装饰性描边」处理，必须 ≥3:1 |

`--glass-blur` 继续统一为 `10px`，没有新增任何超过 10px 的毛玻璃。

### 对比度实测（WCAG 2.x 相对亮度公式，含 alpha 合成）

24 项全部达标（13 项文本类 ≥4.5:1，11 项非文本类 ≥3:1）。关键几项：

| 用途 | 色值 | 背景 | 对比度 | 要求 |
|---|---|---|---|---|
| 日期数字 | `#f4f5ff` | 日期格 | 18.06:1 | ≥4.5 ✅ |
| 任务标题 | `#f4f5ff` | 任务卡 | 14.91:1 | ≥4.5 ✅ |
| 今天数字 | `#ffb36b` | 日期格 | 10.23:1 | ≥4.5 ✅ |
| 任务数量徽标 | `rgba(232,236,255,.72)` | 日期格 | 8.39:1 | ≥4.5 ✅ |
| 完毕徽标文字 | `#f4f5ff` | 任务卡 | 14.91:1 | ≥4.5 ✅ |
| 次级小字（截止日/进度） | `rgba(232,236,255,.72)` | 面板 | 8.41:1 | ≥4.5 ✅ |
| 三级小字（副标题/提示） | `rgba(232,236,255,.55)` | 面板 | 5.38:1 | ≥4.5 ✅ |
| 逾期 / 内联报错 | `#ff6b6b` | 面板 | 6.53:1 | ≥4.5 ✅ |
| 选中日期边框 | `#7c8cff` | 日期格 | 6.06:1 | ≥3 ✅ |
| 聚焦环 | `#7c8cff` | 页面底 / 面板 | 6.58 / 6.08:1 | ≥3 ✅ |
| 控件轮廓 | `rgba(255,255,255,.38)` | 面板 / 日期格 | 3.55 / 3.57:1 | ≥3 ✅ |
| 任务圆点 | `#b3baff` | 日期格 | 9.78:1 | ≥3 ✅ |
| 全完成圆点 | `#8be0a4` | 日期格 | 11.42:1 | ≥3 ✅ |
| **面板边界** | `rgba(124,140,255,.62)` | 页面底 / 面板 | **3.18 / 3.10:1** | ≥3 ✅ |

焦点环沿用 v1.1.1 的全局 `:focus-visible`（`2px` 强调色 + `2px` 外扩），月历内所有格子、
按钮、输入框、复选框自动获得同一套环，没有另写样式。

> 说明：月历在**屏保模式**下也是可见可用的，所以刻意没有加 `.no-fs`；
> 也刻意避开了 `.toggle-subtasks` / `.subtask-panel` 这两个类名
> —— 它们在 `body.screensaver` 下被强制 `display:none`，复用会导致屏保里面板直接消失。

---

## 五、响应式

- 月历网格用 `repeat(7, minmax(0, 1fr))`，**窄屏不会出现横向滚动**（480px 视口已实测）。
- ≤560px 时：格子高度 52→44px、间距收窄到 `--space-1`、「今天/‹/›」换到第二行、
  表单按钮改为整宽纵向排列。
- 超宽屏（2560px）下月历与卡片一样受 `--content-max: 1400px` 限制并居中。
- 底部添加区在宽屏是 `position: sticky`，会盖住面板最下面的控件，
  已用 `scroll-margin-bottom` 让面板/表单滚入视图时自动留出余量。

---

## 六、为什么没改后端（`saver_window.py`）

`Api.read_flags()` 是**原样返回 `flag_data.json` 的文本**，`Api.write_flags(text)` 是**原样写回**，
没有字段白名单、没有 schema 校验。所以 `plan_date` 这类新字段天然就是通的，不存在
「后端把字段过滤掉」的问题。本次只把 `APP_VERSION` 从 `1.1.1` 改成 `1.2.0`
（保证日志 banner、设置面板里显示的版本号与发布包一致），并补了一段说明注释，逻辑零改动。

---

## 七、验证

- **语法**：抽出 `<script>` 单独跑 `node --check` 通过。
- **功能自测**：无头 Edge 自动化脚本 **102 项断言全部通过**，覆盖：
  首次运行（无数据）/ 老数据（无 `plan_date`）加载、月历渲染与 roving tabindex、
  点日期开面板与焦点进出、表单三类内联校验、保存后列表与月历同步、
  同一天多条任务、面板内完成/删除与卡片区一致、空日期空状态、
  `localStorage` 与 `flag_data.json` **两条存储路径的刷新后持久化**、
  Esc 优先级（页面模式与 pywebview 模式各测一遍，含 `quit_saver()` 调用次数）、
  `Ctrl+Alt+G` 不被前端拦截、键盘全流程（方向键/Home/Enter/Esc）、
  跨月/跨年/闰年（2028-02 为 29 天）/月末收敛（1/31 → 2/28）、
  过去日期查看但拦截添加、三档视口无横向滚动、屏保模式下月历与面板仍可见。
- **对比度**：脚本从 `:root` 读 Token 现算，24/24 达标（见上表）。
- **截图回归**：`screenshot.png`（主界面含月历）与新增的 `screenshot_calendar.png`（月历 + 当天面板特写）。

## 八、升级提示

- 直接覆盖 `flag.html` 即可获得新功能（`FlagSaver.exe` 与 `flag_data.json` 都不用动）。
- 第一次打开时旧数据会在文件里多出一个 `"plan_date": ""` 字段，属正常现象，
  旧 Flag 的日期与显示完全不变。
- 如果你不希望某条 Flag 出现在月历上：把它的 `plan_date` 设成一个你不会去看的日期即可
  （当前版本的「缺少 `plan_date` 时回退到 `date`」是为了让旧数据不消失，属有意设计）。
