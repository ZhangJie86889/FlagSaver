# FlagSaver v1.1.1 更新说明

> 本次只改健壮性与排障体验，**没有改界面和交互方式**，用户侧感受是"以前按不动的现在能用了"。

## 修复清单

### 1. 重复双击 exe 毫无反应 → 改为把已有窗口呼出来

**原因**：`already_running()` 命中单实例后直接 `sys.exit(0)`，不提示、不唤醒已有窗口、不写日志。
用户看到的就是"点了启动失败"，而实际上程序一直在后台正常运行。

**修复**：单实例用「互斥量 + 命名事件」：

- 第二个进程发现已有实例时，`SetEvent` 通知它，然后自己退出；
- 已有实例的监控线程读到事件后把窗口显示出来。

真机验证（同一台机器，两个真实进程）：

```
[实例1] 2026-09-15 00:17:02 hotkey active: win+shift+g (配置 ctrl+alt+g) <- 被占用，已自动顺延
[实例2] 2026-09-15 00:17:16 已有实例在运行 -> 已通知它呼出窗口，本进程退出
[实例1] 2026-09-15 00:17:16 收到另一个实例的呼出请求
[实例1] 2026-09-15 00:17:16 show_window: done
```

### 2. 热键可能"用着用着就哑掉"

**原因**：注册热键、跑消息循环、以及调用 pywebview 的窗口方法（`_window.show()/.hide()/.fullscreen`）
全都挤在同一个后台线程里。只要其中一次窗口调用阻塞，这个线程就再也回不到 `PeekMessageW` 循环，
**热键会永久失效**；而空闲自动弹是另一个线程，照常工作 —— 现象极具迷惑性。

**修复**：职责拆开

- 热键线程只做「注册 + 收消息 + 入队」，绝不碰窗口；
- 新增 `window_loop` 专用线程，串行执行所有显隐动作；
- 每个窗口操作放在独立线程里执行并带 8 秒超时，超时写日志而不是把执行线程拖死。

### 3. 异常被静默吞掉

原来 `monitor_loop` / `hotkey_loop` / `_log` 里几乎每个 `except` 都是 `pass`，
程序出问题时既不死、也不报、也不留痕。现全部改为写日志。

### 4. 日志不带日期

`flagsaver.log` 原来只写 `时:分:秒`，跨天完全无法定位。现改为 `年-月-日 时:分:秒`，
并在每次启动写一行版本 banner。

### 5. 热键被占用自动顺延后，用户不知道实际生效的是哪个键

- 实际注册成功的键写入 `config.json` 的 `hotkey_active`；
- 新增 `hotkey_active` 字段参与配置读写；
- `flag.html`：设置面板的输入框**直接显示实际生效的键**（原来显示的是那个不可用的键，极易误导），
  并附一句"直接点保存即固定这个键"；
- `flag.html`：程序启动时若发生顺延，**弹一次 toast** 告知，不必等用户打开设置面板才发现；
- 顺延时日志里多一行明确建议。

### 6. 单实例互斥量命名空间

`Global\FlagSaverSingleton` → `Local\FlagSaverSingleton`。
`Global\` 命名对象在非管理员账户下可能创建失败（`GetLastError = 5` 而非 `183`），
会让单实例保护形同虚设。

### 7. 顺延备选热键表

原表 `ctrl+alt+g / ctrl+alt+f / ctrl+shift+g / ctrl+alt+d / win+alt+g / win+shift+g / ctrl+alt+s / ctrl+alt+1`
里，`ctrl+alt+单字母` 在中文输入法、截图工具、QQ/微信、游戏启动器里普遍被占用，顺延过去基本等于白试。
现改为优先冷门组合：

```
ctrl+alt+g, win+shift+g, ctrl+shift+g, alt+shift+g,
ctrl+alt+f9, ctrl+shift+f9, win+shift+f9, ctrl+alt+q
```

## 验证

- **逻辑单测**（用 `webview` 打桩，不需要 GUI）：19 项全过，覆盖配置读写、热键解析、
  请求队列上限、单实例 + 唤醒事件往返、`hotkey_active` 落盘、无 `config.json` 时不崩。
- **真机验证**：上面第 1 条的日志即两个真实进程的实测输出。
- **对照实验**：把补丁里那几行 `ctypes` 句柄声明的 `restype/argtypes` 删掉后进程会静默死亡
  —— 说明 64 位句柄必须显式声明，否则会被截断。

## 兼容性

- 配置文件向后兼容：老 `config.json` 缺 `hotkey_active` 会按默认值补上，不影响原有设置。
- 未改动 `flag.html` 的界面结构、样式与数据格式，`flag_data.json` 完全不受影响。

---

# 第二批：视觉设计 · 交互反馈 · 可访问性（`flag.html`）

> 目标是在**不改变暗色毛玻璃风格**的前提下，把散落的样式收敛成设计 Token，
> 并把对比度、焦点、键盘操作这些「看不太出来但很影响体验」的地方补齐。
> 唯一的行为变化见文末「行为变化」小节。

## 一、设计 Token

在 `:root` 建立三组基础 Token，组件样式尽量从这里取值：

| 类别 | Token |
|---|---|
| 间距（4px 基准网格） | `--space-1..12`（4 / 8 / 12 / 16 / 20 / 24 / 32 / 48） |
| 圆角 | `--radius-sm/md/lg/xl/pill` |
| 语义色 | `--color-bg-primary`、`--color-text-primary/secondary/tertiary`、`--color-accent`、`--color-danger/warn/success` |
| 边框 | `--color-border`（装饰）、`--color-border-control`（控件轮廓，需 ≥3:1）、`--color-border-hover` |
| 毛玻璃 | `--glass-bg`、`--glass-bg-strong`、`--glass-border`、`--glass-blur`、`--glass-saturate` |
| 焦点 | `--focus-width`、`--focus-offset` |
| 布局 | `--content-max`（1400px，超宽屏限宽居中） |

旧的短变量名（`--bg` / `--a1` / `--t1` / `--glass` …）保留为别名指向新 Token，
避免一次性大范围改名带来的回归风险；新代码一律用语义名。

## 二、可访问性（这轮的主要价值）

### 1. 对比度

实测修复前的问题值：

| 元素 | 修复前 | 修复后 |
|---|---|---|
| `--t3`（次级小字：截止日期、进度、提示行） | `.38` → **3.2:1** ❌ | `.55` → **5.5:1** ✅ |
| `.storage-note` 底部小字 | `.26` → **2.1:1** ❌ | 次级色 → 5.5:1 ✅ |
| 完成态卡片（`.card.is-done`） | 整体 `opacity:.55`，叠上小字后约 **2.4:1** ❌ | 去掉整体降透明，改用背景/文字色分级 → ≥4.5:1 ✅ |
| 表单控件边框 | `rgba(255,255,255,.11)` → **1.3:1** ❌ | `--color-border-control` → **3.5:1** ✅ |
| `::placeholder` | `.3` → 约 2.4:1 | `.5` → ≥4.5:1 ✅ |

- 正文与背景 ≥ 4.5:1；时钟等大号文字 ≥ 3:1；图标/边框等非文本元素 ≥ 3:1。
- 装饰性分隔线（卡片描边等）保持低对比度，不受 1.4.11 约束，毛玻璃观感不变。

### 2. 键盘焦点

- 原先所有输入框都写了 `outline: none`，键盘用户几乎看不到焦点在哪。
- 现在统一 `:focus-visible` 焦点环：`2px` 强调色（对底色 6.6:1）+ `2px` 外扩，
  并覆盖按钮、输入框、复选框等全部可交互元素。

### 3. 不再只靠颜色传达状态

- 「已完成」由原来的淡出 + 删除线，升级为 **颜色 + 对勾图标 + 「已完成」文字** 三重表达，
  色盲/色弱用户同样能识别。
- 子任务进度条补上 `role="progressbar"` + `aria-valuenow`。

### 4. 减少动态效果

- `@media (prefers-reduced-motion: reduce)` 已存在，这轮补上 `scroll-behavior: auto`
  并显式停止背景光晕漂移（`.orb`）。

### 5. 毛玻璃的性能取舍

- 所有 `backdrop-filter` 统一走 `--glass-blur: 10px`（原来分别是 14/16/18px），
  全屏大面积模糊的每帧开销明显下降；面板底色本就是接近不透明的深色，观感几乎无差别。
- 装饰性光晕的 `filter: blur(100px)` 保留 —— 它是静态渐变层，且 `reduce` 模式下动画已停。

### 6. 其它无障碍细节

- 新增「跳到 Flag 列表」跳转链接（`skip-link`）、若干 `sr-only` 标签、
  `role="status" aria-live="polite"` 的 toast 与状态区、图标按钮补 `aria-label`、
  展开按钮补 `aria-expanded` / `aria-controls`。

## 三、表单与交互反馈

| 改动 | 说明 |
|---|---|
| **内联校验** | 添加 Flag 时，错误信息显示在**对应输入框正下方**（`role="alert"`），不再是只弹一句 toast；失焦/输入时即时更新 |
| **过去日期拦截** | 截止日期早于今天会直接标红并阻止提交，提示里带上今天日期 |
| **子任务空值** | 保持抖动提示 |
| **设置面板校验** | 空闲分钟数、热键格式、定时格式都有内联错误；热键失焦即校验（与后端 `parse_hotkey` 规则对齐） |
| **保存按钮状态** | 没有改动时**禁用**；提交中变「保存中…」并禁用两个按钮，防止重复点击 |
| **取消语义** | 有未保存改动时先 `confirm` 二次确认，避免误触丢配置 |
| **焦点管理** | 打开面板自动聚焦第一个字段、`Tab` 在面板内循环（焦点陷阱）、关闭后焦点还原到齿轮按钮 |
| **快捷键提示** | 添加区下方标注「Enter 添加 / ESC 进入屏保」；设置面板底部标注「Enter 保存 / Esc 关闭」 |

## 四、响应式与超宽屏

- 卡片网格改为 `repeat(auto-fill, minmax(min(315px, 100%), 1fr))`：
  窄屏自然堆叠、宽屏自动增列，且内层 `min()` 保证不会撑破窄屏。
- 内容区 `--content-max: 1400px` 并居中 —— 2560px 超宽屏下不会把四张卡片拉得稀稀拉拉。
- 时钟等核心信息原本已用 `clamp()`，保持不变。

## 五、行为变化（请留意）

1. **新增 Flag 时不再接受早于今天的截止日期**（会内联报错并阻止提交）。
   已存在的逾期 Flag 不受影响，仍照常显示「已逾期 N 天」红色状态。
   如果你希望保留「补录一个已经逾期的 Flag」的能力，把 `dateFieldError()` 里
   `left < 0` 那一段去掉即可。
2. **设置面板「取消」在有改动时会弹确认框**；没有改动时直接关闭。

## 六、验证

- `node --check` 通过（提取 `<script>` 单独做语法校验）。
- 无头 Edge 截图回归：1920×1080、2560×1440、480×900 三档尺寸渲染正常，
  卡片网格列数、完成态徽标、输入框边框、超宽屏限宽居中均符合预期。
- 设置面板截图确认：打开即显示焦点环、未改动时「保存」为禁用态、快捷键提示可见。
- 注入 4 条示例数据（含已逾期、已完成）逐项核对状态样式。

